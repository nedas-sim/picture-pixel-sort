# Picture Pixel Sort

## Comments

- Directories for input/output images need to set up manually
- Unclear csv file structure
- Unclear console logs
- .NET Framework 4.6.1 -> .NET 10
- Running on better hardware compared to when the project was first made

# Algorithms

## Merge sort (in-memory, array)

- Same code runs 5 times faster

## Merge sort (in-memory, linked list)

- Same code runs ~3 times faster

## Merge sort (file system, array)

- Crashes because of incorrect opening and closing of files

## Merge sort (file system, linked list)

- Crashes because of incorrect opening and closing of files